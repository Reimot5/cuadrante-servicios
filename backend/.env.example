# Configuración del Backend
# Copia este archivo a .env y ajusta los valores según tu entorno

# Puerto del servidor
PORT=5000

# Entorno de ejecución
NODE_ENV=development

# Secretos JWT
# IMPORTANTE: Cambia esto en producción por un valor seguro
# Genera uno con: openssl rand -base64 32
JWT_SECRET=desarrollo-secreto-cambiar-en-produccion
JWT_EXPIRES_IN=7d

# Base de datos
# Para desarrollo local con SQLite:
DATABASE_URL="file:./prisma/dev.db"

# Para producción con PostgreSQL (Docker):
# DATABASE_URL="postgresql://cuadrante:cuadrante_password@db:5432/cuadrante_db?schema=public"

# Para producción con PostgreSQL (local):
# DATABASE_URL="postgresql://usuario:password@localhost:5432/cuadrante_db?schema=public"

# CORS - Origen permitido para el frontend
# Desarrollo local:
CORS_ORIGIN=http://localhost:5173

# Producción (Docker):
# CORS_ORIGIN=http://localhost:5005

# Producción (con dominio):
# CORS_ORIGIN=https://tu-dominio.com
